server:
  port: 8080

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://192.168.2.128:3306/adminlte?useUnicode=true&characterEncoding=utf8&characterSetResults=utf8&serverTimezone=GMT
    username: root
    password: 1234

# 允许捕获404异常，同时静态资源不可访问
#  mvc:
#    throw-exception-if-no-handler-found: true
#  resources:
#    add-mappings: false

pagehelper:
    helperDialect: mysql
    reasonable: true
    supportMethodsArguments: true
    params: count=countSql
